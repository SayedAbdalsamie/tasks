def find_unique_elements(input_list):
    unique_list = []
    for item in input_list:
        if item not in unique_list:
            unique_list.append(item)
    return unique_list

# Get input for the list of elements
input_list = list(map(int, input("Enter a list of elements separated by spaces: ").split()))

unique_elements = find_unique_elements(input_list)
print("List with unique elements:", unique_elements)
